import type { IQuestionStoreData } from './types'

export
const questionStoreInitial: IQuestionStoreData = {
    steps:    undefined,
    isFirst:  false,
    current:  undefined,
    next:     undefined,
    prev:     undefined,
    question: undefined
}
