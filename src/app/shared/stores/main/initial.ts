import type { IMainStoreData } from './types'

export
const mainStoreInitial: IMainStoreData = {
    select:  true,
    result:  false,
    edit:    undefined,
    flow:    undefined,
    answers: {},
    steps:   {}
}
